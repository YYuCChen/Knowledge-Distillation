# 工程协作规则

后续开发从公开仓库当前分支开始。先检查工作区和已有未提交改动，不把旧私人工程或其 Git 历史整体搬入公开仓库。

先读 [文档入口](docs/README.md)，再读任务相关的小节。代码与测试描述实现，明确的产品裁决描述目标，探索记录描述开放问题。未实现的构想不是垃圾。

开工明确目标、受影响行为、源码提交和必要验证，优先复用现有边界，控制改动范围。如实记录失败与不确定性。

测试必须显式指定独立可丢弃数据目录，不使用私人数据库、Vault、浏览器资料或凭据作为夹具。开发启动命令默认可能指向正式数据位置，不能省略测试数据参数。

来源事实、来源型知识、AI 新知、用户判断保持不同身份，保护证据、归属和已有笔记不被覆盖。探索方向不能冒充已实现能力或发布承诺。

退役旧目录前先保全有效裁决、未来构想、未决事项和独有未提交文件。保留结论与必要证据，不永久保留每轮环境、模型、数据库和打包副本。遵守 [存储规则](docs/engineering/storage.md)。

行为变化同步维护对应规范与必要回归。收尾交代改动、验证和未决事项，再清理本任务确认无用且可重建的临时产物。公开发布、秘密变更及不可逆用户数据操作按明确任务授权执行。
