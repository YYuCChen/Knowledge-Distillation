# 已提出的版本优化

更新：2026-09-13。这里分别记录已实现事项、待做事项及验收目标，以各条状态为准。后续实现必须核对当时最新代码，避免把旧缺陷无条件重开或按版本号自动关闭。

## BUG-20260912-01：来源整理局部纠错失败的留证、恢复与提示

状态：已在 V1.2（构建 2026.09.13.3）实现并发布；下列原始验收要求保留用于追溯。原始运行记录包含真实知识数据，保留本地；这里只记录通用问题与验收要求。

问题：局部纠错候选因引用、定位或结构不合法而被拒绝时，当前失败提示不足以帮助判断原因，也不能用放松事实校验的方式解决。

目标行为：

1. 保留必要的原转写、模型响应及具体失败索引/字段，便于定位。诊断数据留本地，按问题处理与保留期限管理，不能上传真实内容作为公开夹具。
2. 证据引用必须与来源中的连续原文匹配；需要多段证据时分别保存位置与验证，不把拼接片段伪装成一段原文。
3. 针对明确失败原因进行一次有界修复重试。仍不合法时，保留对应原文并标明未解决；定位不可靠则保留完整原段，不能猜改或丢失来源。
4. 用户提示准确说明原文已保留、还有哪些必要核对，不以笼统失败覆盖实际结果。
5. 未经验证的修复不成为正式事实；不能无限重试，也不能因局部失败阻止其他可安全继续的部分。

验收用合成样本覆盖：拼接引用、字面不匹配、区间重叠、定位失败、重试仍失败。确认原文保留、无非法事实入库、诊断可定位、重试有上限、其他安全分支不受阻，并运行相关回归。

该待办不授权重新处理任何真实用户任务，也不把某次人工放行推广成自动通过规则。

## F-10：Windows 更有效的差量更新

状态：已在构建 2026.09.14.1 实现并发布 Windows format 2 文件内二进制差分；真实冻结主 EXE 与 helper 使用内部补丁，原生还原及失败回退通过。保留[原始要求](../history/2026-09-project-memory/windows-delta-request.md)。

原文件级策略遇到大型可执行文件变化会重传整文件。本次采用 HDiffPatch，根据实际压缩后收益选择 reuse/full/patch，精确核验目标树；首版保留双清单兼容方案，不承诺固定补丁体积。

验收要求：普通小修复不再重传整个大型可执行文件；补丁签名、基线匹配有效；重建后的逐文件摘要等于目标完整包。覆盖下载损坏、补丁不适用、重建中断、新版启动失败及旧版恢复，继续保护用户数据/Vault、更新真实性和由用户选择的整包回退。

不能只凭“比完整包小”或合成二进制实验宣称优化完成；也不承诺固定补丁体积。

## V1.2 联合实施状态（2026-09-13）

BUG-20260913-05 统一来源链审计统筹 BUG-20260912-01、BUG-20260913-02（字幕优先与分段识别恢复）、BUG-20260913-04（口语补词）；BUG-20260913-03 单独修正等待区顺序。新增 BUG-20260913-06 为 Apple Vision 极小浮点越界恢复。详见 [V1.2 行为与验收矩阵](../releases/v1.2/README.md)。本轮已随 V1.2 发布，既有现场原件仍在本地保护。该段记录首次 V1.2 交付；F-10 后于构建 2026.09.14.1 完成文件内二进制差分，见上方最新状态。

## BUG-20260914-01：成功安装后的临时组装副本清理

状态：用户于 2026-09-14 明确移至下一轮 bug 迭代，本次不返工、不重新构建。

成功安装后，安装器和更新 helper 的本次基座/候选组装副本可能继续占用数 GB 磁盘；可复用下载缓存与这些临时副本应分别处理。当前未修复此项。下一轮仅清理本次创建、身份仍匹配且成功验收后的临时目录；失败恢复所需资料、已安装应用、模型、可复用下载缓存及用户知识数据必须保留。覆盖安装器与 helper、崩溃恢复、重试、路径替换与清理失败；清理失败不能把已接受安装改判失败。本地待续补丁只作草案，不代表已验证或已交付。


## 2026-09-15：Dock 已有页面聚焦（用户延期）

V1.3 在 Chrome 产品标签仍打开、但切到其他标签时，点击 Dock 可能无法聚焦；重试同样可能无效。已确认关标签五次均恢复、关窗口能恢复，不能据此声称已有页聚焦通过。用户明确要求本轮不修复，下一轮处理。

下轮评估浏览器精确标签控制与所需权限，在用户批准前不新增自动化、辅助功能或扩展授权。保留原标签的输入与路由，不以另开页面冒充找回原页。退出 Chrome 和残余 Safari Dock 场景本轮未验，不把可能成功写成通过。
