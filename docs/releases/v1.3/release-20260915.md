# V1.3 · 2026.09.15.6

同一素材内判断等价的疑点只显示第一张普通疑点卡，一次判断同步组内全部位置。疑点卡沿用 V1.2 定稿设计，不增加汇总、位置勾选或新的判断步骤。不同含义的同字疑点仍独立处理。

本版改进来源身份和修订审计、人工队列并发与恢复、跨页草稿保留，以及安装接受后的收尾和失败恢复。飞书真实回调同步组内决定，并保留正确的操作来源。既有定稿 UI 和交互的后续修改须先取得用户同意。

## 安装与升级

Mac Apple Silicon / macOS 14+ 安装器约 33 MB；Windows x64 安装器约 46 MB。首次安装另外下载平台基座与 Docling；固定资源继续由 `components-2026.09.13` 提供。两平台完整包分别为 536,118,452 与 841,725,116 字节，供离线备用。

已使用组件版的用户可检查更新，Mac 本次差量 5,448,342 字节，Windows 6,775,631 字节。早期全量版需手动从发行页下载安装器并选择原程序与数据目录；旧 Windows 更新器不支持 informational 更新源，不能据此期待它显示迁移入口。不要同时运行两个版本或使用旧版打开新版数据。

Mac 仍未公证，Windows 仍未作发行者代码签名；不要关闭系统整体安全防护。Windows 10 22H2 为本轮平台，Windows 11/ARM64 未验。Python 运行链继续为 CPython 3.11.16。

## 验收范围与限制

本轮区分源码测试、真实冻结程序、本人操作及发布后下载核验。Mac 冻结包一次决定同步8处、异义第9处保留、跨页草稿保留、真实飞书回调均通过；系统中文输入法与安装器原生目录选择、取消、回填已由本人确认。Mac 最终冻结安装器新装通过，三真实旧版迁移与受控回退按未变安装模块的精确来源证据复用。Windows 非核心汇总路径格式和换行差异按用户裁决延期，不宣称历史官方汇总总门通过。

Chrome 已有后台产品标签无法由 Dock 可靠聚焦，重试也可能无效；用户明确移至下一轮，不新增权限。可手动选择原标签。关闭产品标签五次均恢复、关闭产品窗口能恢复；退出 Chrome 及剩余 Safari 场景未验，不能把这部分写成通过。

最终 Windows 核心安装与线上资产读回结果随 [Release](https://github.com/YYuCChen/Knowledge-Distillation/releases/tag/v2026.09.15.6) 更新。真实回调使用独立合成任务，不等于审计或重写用户历史知识；未代确认或重试正式任务。

## 构建来源

发行标签直接对应主程序及更新 helper 的构建源码 `453154ea1999935bd5b8701656b74579099333f5`。独立安装器复用 `2026.09.15.1`，源码 `261e7a2fa223fd59200d7127e3ea5611303376b9`；固定基座与模型未因局部疑点修复重建。发布资产附 SHA256SUMS；后续文档提交不改变二进制身份。
