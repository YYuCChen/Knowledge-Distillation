# 开发与贡献

新开发从核对后的公开 main 建立聚焦的工作分支。检查已有修改，不覆盖其他任务成果，不整体导入旧私人工程。

## 开发与验证

Python 要求与依赖安装以根 README 为准。完整安装包含较大的平台和模型依赖；仅审阅文档时不安装这些依赖。

现有快速回归 workflow 使用有限依赖与选定测试。全量测试、原生包检查、真实来源采集、模型调用及人工阅读验证各有证据范围，不能相互冒充。

本地开发必须明确指定新的测试目录和空闲端口：

```sh
PYTHONPATH=src .venv/bin/python -m knowledge_distiller --data-dir <disposable-data-directory> --port <unused-port>
```

替换两个占位参数，不指向私人数据目录或正式 Vault。已审阅的命令入口使用 Unix 专属设施，该命令不是已验证的 Windows 源码开发方案。

## 提交与审阅

说明具体问题、改后行为及验证。除非任务明确改变产品约定，否则保留现有边界。更新对应文档，不追加第二套平行规格。测试使用合成或获准再分发的样本，按需保留来源和许可。

检查精确暂存文件及将提交的历史。凭据、数据库、浏览器状态、真实来源材料、模型权重、私人笔记、安装包和聊天记录不进入源码提交。ignore 不能移除已跟踪文件。

文档变更检查事实、状态、链接和隐私；未运行测试就明确说明。行为变更运行必要的针对性及集成检查，说明未验证的平台和环境。

收尾按 [存储规则](docs/engineering/storage.md)清理可重建输出；先留下有效决定和未决事项，再清除旧现场。
